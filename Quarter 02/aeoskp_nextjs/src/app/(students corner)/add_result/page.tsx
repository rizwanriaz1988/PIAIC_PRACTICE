import React from 'react';
import PropTypes from 'prop-types';

const page = props => {
    return (
        <div>
            
        </div>
    );
};

page.propTypes = {
    
};

export default page;